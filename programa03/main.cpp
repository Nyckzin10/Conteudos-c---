#include <stdint.h>
#include <stdlib.h>

float vat[20], soma=0, media; int posicao;


for (posicao=0;posicao=<20;posicao++){
    scanf("%f", &vet[posicao]);
    soma=soma+vet[posicao]
}
media=soma/20;
for (posicao=0;posicao=<20;posicao++)
{
    if(vet[posicao] >= media)
    printf("%.2f \m", vet[posicao]);
}